@AGENTS.md

## Claude Code 固有

AGENTS.md に書いてあることはここで繰り返さない。Claude Code だけが解釈できるものに限る。

- ★このディレクトリには `.claude/` の設定もフックも無く、beads も使っていない。
  親ディレクトリ側 (`C:\code\doubutusyougi`) には beads がある。別リポジトリなので issue はそちらで管理する
